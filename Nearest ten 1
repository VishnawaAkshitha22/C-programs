#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
 int n,a;
    scanf("%d%d",&n,&a);
    a=n%10;
    if(a>=0 && a<=10){
        
    
    if(a>=5 && a<10)
    {
        printf("%d",n+(10-a));
    }
        else{
            printf("%d",n-a);
        }
    }  
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
