#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int a[5],b[5],i,j,n1,n2;
    scanf("%d%d",&n1,&n2);
    if(n1==n2)
    {
    for(i=0;i<n1;i++)
    {
        scanf("%d",&a[i]);
    }
    for(j=0;j<n2;j++)
    {
        scanf("%d",&b[j]);
    }
     int flag=0;
    for(i=0;i<n1;i++)
    {
    if(a[i]!=b[i])
        {
            flag=1;
            break;
        }
        
    } if(flag==0){
        printf("Same");
    }
        else{
            printf("Not Same");
        }
    }
    else{
        printf("Not Same");
    }
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}

